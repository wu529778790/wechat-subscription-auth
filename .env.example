# 网站地址（必须，微信回调用）
SITE_URL=https://your-site.com

# SDK API Base URL（前端 SDK 使用，默认线上地址）
API_BASE=https://auth.shenzjd.com

# 微信后台配置（必须）
WECHAT_TOKEN=your-wechat-token

# 公众号名称（必须，前端显示）
WECHAT_NAME=神族九帝

# 公众号二维码URL（可选，留空显示默认占位图）
WECHAT_QRCODE_URL=https://gcore.jsdelivr.net/gh/wu529778790/image/blog/qrcode_for_gh_61da24be23ff_258.jpg

# Session密钥（必须，生产环境用随机字符串）
# 生成命令：openssl rand -hex 32
SESSION_SECRET=dev-secret-change-in-production

# 以下仅认证服务号需要（个人订阅号留空）
WECHAT_APPID=
WECHAT_APPSECRET=

# 存储类型（可选，默认JSON，可选：sqlite）
# STORAGE_TYPE=sqlite

# 验证码有效期（可选，默认300秒）
# CODE_EXPIRY=300

# 关键词配置（可选，JSON格式）
# KEYWORDS=["验证码"]
