# ============================================
# 微信订阅号认证系统 - 环境变量配置
# ============================================

# ============================================
# 1. 网站配置（必须）
# ============================================
# 网站访问地址（用于生成完整链接）
SITE_URL=https://your-domain.com

# Session加密密钥（生产环境必须使用强随机字符串）
# 生成命令: openssl rand -hex 32  (Windows: 在 Git Bash 中执行)
SESSION_SECRET=your-random-secret-key-change-this

# ============================================
# 2. 微信公众号配置（必须）
# ============================================
# 服务器 Token（与微信后台配置一致，**最重要**）
# 建议使用复杂字符串，如: wx_auth_token_2024_xxxxx
WECHAT_TOKEN=your-token

# ============================================
# 3. 消息加解密配置（安全模式需要）
# ============================================
# 公众号 AppID（必须，安全模式需要）
WECHAT_APPID=

# 公众号 AppSecret（未认证订阅号无需填写）
WECHAT_APPSECRET=

# EncodingAESKey（安全模式需要，43位字符）
# 在微信后台"基本配置"页面生成或手动填写
# 格式：43位字母数字组合（包含 / + = 等Base64字符）
WECHAT_AES_KEY=

# ============================================
# 4. 可选配置
# ============================================
# 验证码有效期（秒），默认300秒（5分钟）
# CODE_EXPIRY=300

# 轮询间隔（毫秒），默认3000毫秒（3秒）
# POLL_INTERVAL=3000

# 轮询超时时间（毫秒），默认300000毫秒（5分钟）
# POLL_TIMEOUT=300000

# 存储类型：file（默认JSON文件）或 sqlite（需编译原生模块）
# STORAGE_TYPE=file

# ============================================
# 4. 多公众号配置（可选，JSON格式）
# ============================================
# 如果需要支持多个公众号，使用此配置（覆盖单公众号配置）
# 格式: [{"name":"公众号名称","appId":"...","appSecret":"...","token":"..."}]
# WECHAT_ACCOUNTS=[{"name":"测试号","appId":"wx123","appSecret":"","token":"token123"}]
